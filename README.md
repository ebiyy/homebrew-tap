# Homebrew Tap

Custom Homebrew tap for ebiyy's apps.

## Installation

```bash
brew tap ebiyy/tap
```

## Available Casks

### TrayLingo

macOS menu bar translation app powered by Claude AI.

```bash
brew install --cask ebiyy/tap/traylingo
```

**Note:** Currently ARM (Apple Silicon) only.
